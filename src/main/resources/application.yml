spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: 127.0.0.1
          search-paths: /learn
          username: asd
          password: sad

server:
  port: 7001
