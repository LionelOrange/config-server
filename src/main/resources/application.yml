spring:
  application:
    name: ja
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LionelOrange/config-server
          search-paths: config
          username: xx
          password: xx

server:
  port: 7001
