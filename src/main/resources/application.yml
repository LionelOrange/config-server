spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LionelOrange/config-server
          search-paths: config
          username: xxx
          password: xxx

server:
  port: 7001
